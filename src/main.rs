#![feature(iter_array_chunks)]

pub mod day;

fn main() {
    println!("Advent of code");
    // day::one::run().unwrap();
    // day::two::run().unwrap();
    // day::three::run().unwrap();
    // day::four::run().unwrap();
    day::five::run();
}
